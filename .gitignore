# Python
__pycache__/
*.py[cod]
*.pyo
venv/

# Databases (runtime data, not source)
*.db

# Claude Code session data (prompts, memory, settings — never commit)
.claude/

# Local credentials — never commit
.env
.scan.lock
